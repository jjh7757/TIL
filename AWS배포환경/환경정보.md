# 환경 정보

실제 리소스 식별자 모음. 템플릿의 플레이스홀더를 채울 때 여기서 가져온다.

> 계정 ID·인스턴스 ID·리전은 비밀값이 아니다(IAM 정책과 ARN에 늘 드러난다).
> 다만 이 저장소를 공개로 둘 거라면 굳이 공개할 이유도 없으니, 커밋 전에 한 번 판단할 것.
> **비밀번호·액세스 키·`.env` 값은 절대 여기에 적지 않는다.**

## 계정

| 항목 | 값 |
|---|---|
| 계정 ID | `403187831140` |
| 리전 | `ap-northeast-2` (서울) |
| 콘솔 로그인 URL | https://403187831140.signin.aws.amazon.com/console |
| 관리자 IAM 사용자 | `jjh-admin` |

## 서버

| 항목 | 값 |
|---|---|
| 인스턴스 ID | `i-05ee0665bbba15a52` |
| 이름 | `deploy-server` |
| 유형 | `t4g.small` (2 vCPU / 2GB, Graviton2 arm64) |
| AMI | `ami-05cd5f557a0769fae` — Ubuntu Server 24.04 LTS arm64 |
| 기본 사용자 | `ubuntu` |
| 스토리지 | gp3 30GB |
| Elastic IP | `13.209.128.48` (alloc `eipalloc-0140406fd750507e3`) |
| 도메인 | `test.ai-agent-develop.cloud` — 가비아 DNS, A 레코드 |
| 접속 | Session Manager (SSH 22는 닫혀 있음) |

## 보안 그룹 / IAM

| 항목 | 값 |
|---|---|
| 보안 그룹 | `web-server-sg` — 인바운드 80/443만 |
| EC2 역할 | `ec2-deploy-target` — `AmazonSSMManagedInstanceCore` + `ecr-pull` |
| Actions 역할 | `github-actions-deploy` *(3단계에서 생성)* |
| 키 페어 | `deploy-key` — 로컬 `~/.ssh/aws_deploy` (안전망용) |

## 파이프라인 검증용 (3단계)

| 항목 | 값 |
|---|---|
| GitHub 저장소 | `jjh7757/deploy-test` (퍼블릭 — ARM 러너 무료) |
| 로컬 경로 | `부트캠프/deploy-test` (TIL 저장소와 형제 폴더) |
| `APP_NAME` | `deploy-test` |
| ECR 리포지토리 | `deploy-test` |
| 서버 디렉토리 | `/srv/deploy-test` |
| Actions 역할 | `github-actions-deploy` |
| GitHub owner ID / repo ID | `307797437` / `1365988315` (신뢰 정책 `sub`에 사용) |
| 상태 | ✅ 2026-09-11 첫 배포 성공 |
| 서비스 URL | https://test.ai-agent-develop.cloud (Let's Encrypt, ~2026-12-10 자동 갱신) |

## 배포된 앱

| 앱 | URL | GitHub | ECR | 서버 |
|---|---|---|---|---|
| `deploy-test` | https://test.ai-agent-develop.cloud | [deploy-test](https://github.com/jjh7757/deploy-test) (repo id `1365988315`) | `deploy-test` (최근 10개) | `/srv/deploy-test` |
| `rag` | https://rag.ai-agent-develop.cloud | [rag-web](https://github.com/jjh7757/rag-web) (repo id `1366999226`) | `rag` (최근 5개) | `/srv/rag` + 볼륨 `rag_index` |

`rag`는 이미지가 약 1.5GB(LangChain·Chroma·Kiwi 모델)라 ECR 보관 개수를 5개로 줄였다.
인덱스는 `rag_index` 볼륨에 두어 재배포 때 임베딩을 다시 하지 않는다
(문서 11건 / 청크 520개, 최초 구축 64초).

## 도메인

`ai-agent-develop.cloud` (가비아 구매, 네임서버는 가비아 기본값).
프로젝트마다 서브도메인을 붙여 한 도메인 아래 모은다.

| 서브도메인 | 가리키는 곳 |
|---|---|
| `whybuy` | `49.50.134.102` — 네이버클라우드 (왜샀어) |
| `test` | `13.209.128.48` — AWS deploy-server (deploy-test) |

HTTPS는 Caddy가 Let's Encrypt에서 자동 발급한다.
**DNS 전파를 확인한 뒤에 Caddy를 reload할 것** — 순서가 어긋나면 발급에 실패하고
Let's Encrypt 실패 한도에 걸려 한동안 재발급이 막힌다.

## 기억할 날짜

| 날짜 | 무슨 일 |
|---|---|
| **2026-12-31** | t4g.small 무료 트라이얼 종료 → 월 약 $15 발생 시작 |
| **2027-01 초** | Budgets 예산을 $10 → $30으로 상향 |
| **2027-08** | 네이버클라우드 Micro Server 무료 종료 → 왜샀어 이관 여부 결정 |
