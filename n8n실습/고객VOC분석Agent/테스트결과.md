# 통합 테스트 결과 (8단계)

모든 케이스는 n8n 수동 실행이 아닌 **Google Form 실제 제출**로 진행했다 (REQ-02 검증 포함).

| TC | 시나리오 | 기대 결과 | 결과 | 비고 |
| --- | --- | --- | --- | --- |
| TC-01 | 정상 문의, 무조작 제출 | 자동 실행 + 결과 1건 저장 | ✅ 통과 | TC-02~06이 전부 폼 제출로만 진행돼 별도 테스트 없이 반복 증명됨 |
| TC-02 | "카드 결제가 막혔어요" (환불·서비스 이용 불가) | `urgency=상` + Discord 도달 | ✅ 통과 | Discord 임베드에 물품번호/긴급도/감정/이메일/문의내용/요약 정상 출력 |
| TC-03 | "사이즈 알려주세요" (단순 질문) | `urgency=하` + 알림 없음 | ✅ 통과 | |
| TC-04 | "배송이 하루 늦었어요" (불편·개선 요청) | `urgency=중` + 알림 없음 | ✅ 통과 | |
| TC-05 | TC-02와 동일 내용(이메일·물품번호·문의유형) 재제출 | 행 증가 없음, 알림 없음, 실행 성공 | ✅ 통과 | 최초 시도에서 실패 → 원인: 중복 판정 노드(`Code in JavaScript1`)가 `Get Rows` 결과를 pairedItem 기준으로 잘못 필터링해 실제로 존재하는 중복 행을 못 찾음. 필터링 제거하고 `rowsInput.some(...)`으로 전체 비교하도록 수정 후 통과 |
| TC-06 | 물품번호는 TC-02와 동일, 문의유형만 다르게 제출 | 신규건으로 정상 처리 | ✅ 통과 | 중복키 = 이메일\|물품번호\|문의유형 이므로 유형이 다르면 신규 취급 |
| TC-07 | 프롬프트를 임시로 오염시켜 LLM이 `urgency: "매우높음"` 반환 유도 | `urgency=중`, `needs_review=true`로 강제, 저장됨, 알림 없음 | ✅ 통과 | 5단계에서 확인. 안전판이 허용값 위반을 정상적으로 차단 |
| TC-08 | LLM 응답이 JSON 형식이 아닌 경우 | 기본값으로 저장, 워크플로우 성공 종료 | ✅ 통과 | 안전판 `try/catch`가 파싱 실패를 흡수, 예외 없이 기본값 처리 |
| TC-09 | 문의내용을 길게 작성해 summary가 80자를 넘게 유도 | summary 80자로 절단, `needs_review=true` | ✅ 통과 | |
| TC-10 | 문의유형은 "결제"로 선택했지만 실제 내용은 배송 관련 | `유형일치=false` | ✅ 통과 | LLM이 `category=배송`으로 판단, 고객 선택값과 불일치 기록 확인 |

## 진행 중 발견되어 수정한 이슈 (참고)

구현 과정에서 실제로 겪었던 문제와 조치 내역. 대부분 n8n Code 노드의 **Mode(Run Once for All Items / Each Item)** 와 **pairedItem 추적** 관련이었다.

1. `정규화` 노드 — Mode를 All Items → Each Item으로 변경하며 `return` 문의 배열 래핑(`[{json:{...}}]`)을 제거하지 않아 에러 발생 → 배열 제거로 해결
2. `Code in JavaScript1`(중복키 비교) — All Items 모드에서 `$('정규화').first()`만 참조해 배치 중 첫 번째 아이템만 처리하던 문제 → `$('정규화').all()` + `.map()`으로 전체 아이템 처리하도록 수정
3. 검증 안전판 노드 — 동일하게 All Items 모드로 인해 여러 아이템이 1건으로 뭉개짐 → Each Item 모드 + 배열 없는 `return` 으로 수정
4. TC-05에서 재발한 중복 판정 실패 — `Get Rows` 결과를 pairedItem으로 서브 필터링하다 실제 존재하는 행을 놓침 → 필터링 제거, 전체 목록 대상 비교로 단순화
5. `Basic LLM Chain` 통과 후 원본 필드(`문의내용`, `이메일`, `물품 번호` 등)가 사라지는 것을 확인 → 6·7단계에서 `$('Code in JavaScript1').item.json.*` 로 역참조하여 해결
